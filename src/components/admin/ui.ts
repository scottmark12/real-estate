// Shared class strings so every admin form/table uses the same sharp,
// editorial vocabulary as the public site (no rounded corners, no card
// shadows — thin rules and the site's navy/cream/gold/blue palette).

export const label = "text-sm font-medium text-navy/70";

export const input =
  "mt-1 w-full border border-navy/15 bg-white px-3 py-2 text-sm text-navy placeholder:text-navy/30 focus:border-navy focus:outline-none";

export const textarea = input;

export const select = input;

export const btnPrimary =
  "border border-blue bg-blue px-5 py-2.5 text-sm font-medium text-cream transition-colors hover:bg-blue-dark";

export const btnSecondary =
  "border border-navy px-5 py-2.5 text-sm font-medium text-navy transition-colors hover:bg-navy hover:text-cream";

export const tag = "border px-2 py-0.5 text-[11px] font-medium uppercase tracking-wide";
